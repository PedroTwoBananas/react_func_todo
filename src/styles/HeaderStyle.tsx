import styled from 'styled-components'

export const HeaderWrapper = styled.div`
   margin-top: 32px;
   border: 3px solid white;
   border-radius: 15px;
   padding: 16px;
   display: flex;
   flex-direction: row;
   gap: 10px;
`
